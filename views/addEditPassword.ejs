<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/header') %>
    <link rel="stylesheet" href="/css/styles.css">
    <title>Add/Edit Password - PasswordKeepR</title>
</head>
<body>
    <header>
        <%- include('partials/nav') %>
    </header>
    <main>
        <h1><%= editing ? 'Edit' : 'Add' %> Password</h1>
        <form action="<%= editing ? `/edit-password/${password.id}` : '/add-password' %>" method="POST">
            <input type="text" name="website_name" placeholder="Website Name" value="<%= password.website_name || '' %>" required>
            <input type="text" name="username" placeholder="Username" value="<%= password.username || '' %>" required>
            <input type="password" name="password" placeholder="Password" value="<%= password.password || '' %>" required>
            <select name="category">
                <option value="social">Social Media</option>
                <option value="work">Work</option>
                <option value="entertainment">Entertainment</option>
                <!-- More categories -->
            </select>
            <button type="submit"><%= editing ? 'Save Changes' : 'Add Password' %></button>
        </form>
    </main>
    <footer>
        <%- include('partials/footer') %>
    </footer>
</body>
</html>
